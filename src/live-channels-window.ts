/**
 * Standalone channel management window (LIVE panel: add/remove/reorder channels).
 * Simplified: LiveNewsPanel has been removed.
 */

export async function initLiveChannelsWindow(_containerEl?: HTMLElement): Promise<void> {
  // Simplified: LiveNewsPanel has been removed
  console.log('[LiveChannels] Feature disabled: LiveNewsPanel removed');
}
