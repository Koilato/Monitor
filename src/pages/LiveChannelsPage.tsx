import React from 'react';

export const LiveChannelsPage: React.FC = () => {
  return (
    <div data-testid="channels-page">
      {/* React Channels - will replace live-channels-window.ts in Phase 7 */}
    </div>
  );
};
