import React from 'react';

export const SettingsPage: React.FC = () => {
  return (
    <div data-testid="settings-page">
      {/* React Settings - will replace settings-window.ts in Phase 7 */}
    </div>
  );
};
